include:
    - .postgis          # Installs PostGIS and moves it if needed
    - .osm2pgsql        # Installs OSM2PGSQL, configures for subsequent reloading
    - .update_data      # Fetches and imports OSM data
